# Server Configuration
# Port on which the server will listen
PORT=8080

# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgres://user:password@localhost:5432/pulse?sslmode=disable

# Redis Configuration (optional, for caching)
# Redis connection URL
REDIS_URL=redis://localhost:6379

# Security Configuration
# Secret key for JWT token generation and password reset tokens
# IMPORTANT: Change this to a secure random string in production
JWT_SECRET=change-this-secret-in-production

# ClickHouse Configuration (optional, for analytics)
# ClickHouse connection DSN
CLICKHOUSE_DSN=clickhouse://default@localhost:9000/default

# API Documentation
# Directory containing OpenAPI specifications
API_SPEC_DIR=/app/specs/api

# Email Configuration
# Email backend URL. Supported backends:
#   - smtp://user:password@host:port          - Standard SMTP (port 587, STARTTLS if available)
#   - smtp+tls://user:password@host:587        - SMTP with STARTTLS
#   - smtp+ssl://user:password@host:465        - SMTP over SSL (port 465)
#   - consolemail://                           - Log emails to console (development)
#   - filemail:///path/to/dir                  - Write emails to files (testing)
#   - memorymail:// or locmemmail://           - Store emails in memory (testing)
#   - dummymail://                             - Discard emails silently (testing)
# Default: consolemail://
EMAIL_URL=consolemail://

# Email sender address (required for SMTP backends)
# This will be used as the "From" field in all outgoing emails
EMAIL_FROM=noreply@pulse.com

# Frontend URL
# Base URL of the frontend application, used for generating links in emails
# Example: http://localhost:3000 or https://app.example.com
FRONTEND_URL=http://localhost:3000

# Optional Configuration

# Password Reset Timeout
# Time in seconds before password reset tokens expire
# Default: 259200 (3 days)
PASSWORD_RESET_TIMEOUT=259200

# Region Code
# Region identifier for the deployment
# Default: apac
REGION_CODE=apac
